package com.learnjavaopenjdk12;

import java.awt.*;
import java.text.NumberFormat;
import java.util.Date;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {

        // use the PrimitiveTypes class
        // PrimitiveTypes primitiveTypes = new PrimitiveTypes();

        // use the ReferenceTypes class
        // ReferenceTypes referenceTypes = new ReferenceTypes();

        // use the Strings class
        // Strings strings = new Strings();

        // use the Arrays class
        // Arrays arrays = new Arrays();

        // use the Constants class
        // Constants constants = new Constants();

        // use ArithmeticExpressions
        // ArithmeticExpressions arithmeticExpressions = new ArithmeticExpressions();

        // use the Casting class
        // Casting casting = new Casting();

        // use the MathClass class
        // MathClass mathClass = new MathClass();

        // use the FormatNumbers class
        // FormatNumbers formatNumbers = new FormatNumbers();

        // use the ReadInput class
        // ReadInput readInput = new ReadInput();

        // use the MortgageCalculator class
        // MortgageCalculator mortgageCalculator = new MortgageCalculator();

        // use the FizzBuzz class
        // FizzBuzz fizzBuzz = new FizzBuzz();

        // use the ControlFlow class
        // ControlFlow controlFlow = new ControlFlow();

        // use the MortgageCalculator2 class
        MortgageCalculator2 mortgageCalculator2 = new MortgageCalculator2();
    }
}
